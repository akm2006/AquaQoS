# Continuation scope

Owner requested "continue" after the rejection replay milestone.
Root selected STATUS's same-transaction and finite-allowance checks: reproduce a
guarded rejection and execute comparable unguarded official settlement, matching real
inventory, allowance and virtual balances while preserving original g/baselines.
Do not weaken protection to make the controls pass. Document a safe same-transaction
rejection separately from finite-allowance replenishment risk. Keep independent review
open if reviewer capacity is unavailable; continue local tests/docs. No deployment or
public actions authorized by this continuation.
